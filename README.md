# OutcomeOS

OutcomeOS is a tenant-scoped reference implementation for evidence-gated outcome billing. Domain code lives in `apps/api/app/domains`; the dependency-free dashboard and inbox live in `apps/web`.

Run tests with `pytest`. Preview the web app with `python -m http.server 8000 -d apps/web`.
